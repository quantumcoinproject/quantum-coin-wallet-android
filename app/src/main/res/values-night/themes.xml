<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!--
            See values/styles.xml > AppButtonPlain for the rationale.
            Mirrored here so dark-mode buttons get the same chrome-
            free behavior as light-mode buttons; without it the
            Unlock dialog button would render the Material default
            tint in dark mode while light-mode picked up the
            override.
        -->
        <item name="materialButtonStyle">@style/AppButtonPlain</item>

    </style>
</resources>